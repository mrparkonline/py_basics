# Next in Line

# input
first = int(input('Enter the youngest age: '))
second = int(input('Enter the second child age: '))

# processing
eldest = second + (second - first)

# output
print('The eldest child age is:', eldest)