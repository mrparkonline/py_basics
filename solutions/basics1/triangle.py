# Area of a Triangle

# input
base = float(input('Enter your base dimension: '))
height = float(input('Enter your height dimension: '))

# processing
area_triangle = base * height / 2

# output
print('The area is:', area_triangle, 'units squared.')